ydn64 — RFC REFERENCE LIST
==========================

Authoritative list of the RFCs that ydn64's NAT64 and DNS64 implementations
are measured against, with current implementation status.

Last reviewed: 2026-08-24 (TCP idle expiry per RFC 5382 REQ-5, UDP
Endpoint-Independent Mapping per RFC 4787 REQ-1 / RFC 6146 §3.1/§5.2,
and full RFC 6052 §2.2 prefix formats landed in one session)
Documented deviations: README.md, "Standards conformance and deliberate deviations"

DESIGN GOAL: ydn64 aims to work well for Yggdrasil clients, NOT to be a
strictly conformant NAT64/DNS64. Where the two conflict, ydn64 chooses the
behaviour that works for a client whose only connectivity is Yggdrasil.
Items marked [DEVIATION] are deliberate and are documented in README.

STATUS LEGEND
  [DONE]      implemented and conformant
  [PARTIAL]   implemented for a subset of cases
  [TODO]      needs implementing
  [DEVIATION] deliberately different; documented in README
  [DEFERRED]  wanted, but not scheduled
  [N/A]       out of scope for ydn64's architecture or deployment model


A. CORE NORMATIVE — IMPLEMENT AGAINST THESE
-------------------------------------------

RFC 6146 — Stateful NAT64: Network Address and Protocol Translation from
           IPv6 Clients to IPv4 Servers                            [PARTIAL]
    The primary NAT64 specification. Basic IPv6->IPv4 forwarding for TCP,
    UDP and ICMP Echo works. Outstanding:
      - §3.5 / §5.4  drop inbound IPv6 with a source inside Pref64::/n
                     (anti-hairpin loop): enforced on the TCP, UDP and ICMP
                     paths                                          [DONE]
      - §3.4         fragment handling: TCP/UDP fragments are reassembled
                     by the gVisor netstack before translation and oversized
                     replies are fragmented by it on egress; the raw-socket
                     ICMP Echo path reassembles fragmented Echo Requests in
                     a bounded table and emits oversized replies as IPv6
                     fragments (RFC 8200 §4.5)                       [DONE]
      - §3.5.1 / §4  UDP session lifetime floor (UDP_MIN = 2 min) and
                     default (UDP_DEFAULT = 5 min): Nat64UdpTimeout
                     defaults to 300 s and Validate() enforces >= 120 s [DONE]
      - §3.5.3 / §4  ICMP session lifetime and NAT-allocated ICMP
                     Identifier (RFC 5508 REQ-1/REQ-2): outbound Echo
                     Requests carry a ydn64-allocated identifier mapped to
                     (client, destination, client ID); the client-chosen
                     ID never reaches the IPv4 side, replies restore it,
                     repeat queries reuse their allocation, and the table
                     is capped (maxICMPSessions); idle timeout fixed at
                     60 s                                              [DONE]
      - §5.3         resource limits / anti-DoS: global caps (Nat64Max*),
                     per-source ceilings for UDP sessions and proxied TCP
                     connections (Nat64MaxUDPSessionsPerSource /
                     Nat64MaxTCPConnectionsPerSource), and outbound-only
                     session-lifetime refresh (inbound v4 datagrams no
                     longer pin sockets — the keep-alive behaviour this
                     subsection warns about)                        [DONE]
      - §3.1 / §5.2  Binding Information Base -> Endpoint-Independent
                     Mapping, UDP: one unconnected ListenUDP per client
                     socket (srcAddr6, srcPort) shared across all its
                     destinations; per-tuple sessions are demuxed off the
                     shared socket by the configured filtering policy;
                     global/per-source caps and idle expiry landed together.
                     Unblocks STUN/ICE/WebRTC/QUIC hole punching.
                     (TCP EIM would mean abandoning the gVisor forwarder
                     and stays out of scope.)                            [DONE]
      - §3.5.2       TCP state machine                                  [N/A]
                     (gVisor owns TCP state; idle proxied connections are
                     instead expired wholesale per RFC 5382 REQ-5)
      - §3.8         hairpinning                                        [N/A]

RFC 6147 — DNS64: DNS Extensions for NAT from IPv6 Clients to IPv4 Servers
                                                                   [PARTIAL]
    The primary DNS64 specification. Synthesis, zone rules, forwarding,
    caching and PTR reversal work. Outstanding:
      - §5.1.2       return the upstream NXDOMAIN instead of NOERROR   [DONE]
      - §5.1.5/5.1.7 preserve the CNAME chain; take the synthetic AAAA
                     owner name from the A record                      [DONE]
      - §5.1.7       synthetic TTL = min(A TTL, SOA TTL)               [DONE]
      - §5.1.7       per-IPv4-range prefix selection: special-use IPv4
                     ranges (RFC 1918, loopback, link-local, ...) are
                     excluded from synthesis via IgnoredDstSubnets, but
                     selecting a DIFFERENT configured prefix per IPv4
                     range is not implemented (prefix is chosen per
                     domain zone)                                   [PARTIAL]
      - §5.1         pass through non-IN qclass (no synthesis)         [DONE]
      - §5.1.4       special exclusion set for AAAA                    [TODO]
                     (the RFC-blessed way to express "drop AAAA my
                     clients cannot reach"; could eventually subsume
                     the return-ipv6-addresses zone flag, but that
                     would be a breaking config change)
      - §5.3.2/§5.4  pass through the authority + additional sections
                     of the upstream response (makes SOA-based negative
                     caching work downstream)                          [DONE]
      - §5.4         EDNS(0) handling and truncation / TC bit          [DONE]
      - §5.5         DNSSEC: honour CD=1&&DO=1 (verbatim relay, no
                     synthesis, no cache; upstream AD preserved) and
                     never assert AD on generated/modified data       [DONE]
      - §5.1.1       synthesis when real AAAA exist              [DEVIATION]
      - §5.3.3       A-record suppression                        [DEVIATION]
      - §5.3.1       PTR strategy (resolves in-addr.arpa directly
                     rather than either specified option)        [DEVIATION]

RFC 6052 — IPv6 Addressing of IPv4/IPv6 Translators                 [DONE]
    Address embedding for both NAT64 and DNS64. All §2.2 formats are
    implemented via a shared length-aware helper (config.Pref64): /32,
    /40, /48, /56, /64 and /96, including the u-octet-at-byte-8 rule
    (split-octet layouts for /40-/56), zero-u/zero-suffix canonical-form
    enforcement on extraction, and per-zone prefix selection in DNS64
    (a zone prefix may differ from Nat64Pool). ydn64 derives its Network-
    Specific Prefix from the node's own Yggdrasil /64 as a /96, which
    satisfies §3.2's "own address space" requirement. The Well-Known
    Prefix 64:ff9b::/96 is not configured by default but works if set.

RFC 7915 — IP/ICMP Translation Algorithm                               [PARTIAL]
    Obsoletes RFC 6145. ydn64 does not translate packet headers — it
    terminates and re-originates over OS sockets — so Traffic Class/DSCP,
    ECN, Hop Limit<->TTL and the DF bit are not carried across. Stateless
    full-header translation remains architecturally out of scope.
    §4.2/§4.3 ICMP *error* translation is implemented for the paths ydn64
    has state for: v4-side Destination Unreachable / Time Exceeded /
    Parameter Problem quoting a live UDP session or allocated Echo
    identifier are demuxed, mapped per the §4.2 type/code tables (PTB MTU
    = max(1280, min(v4MTU+20, yggMTU)), RFC 1191 plateau fallback for
    MTU=0, Figure 3 pointer remapping), rebuilt with the inner tuple
    restored to the client's original address/port, truncated to the
    1280-byte budget and injected via ipv6rwc: UDP PMTUD and flow-directed
    error reporting (closed ports, too-small MTU, admin prohibition) work
    through NAT64 as a result. Classic hop-by-hop traceroute stays limited
    by architecture — re-originated datagrams start with a fresh IPv4 TTL,
    so early-hop Time Exceeded never occurs; errors aimed at the flow
    itself translate normally. TCP errors are excluded by design (the OS
    stack consumes them for its own sockets); RFC 4884 ICMP extensions in
    quotes are truncated rather than adjusted.
    Error-translation scope fully implemented; stateless header
    translation remains out of scope by architecture.                [PARTIAL]

RFC 8200 — Internet Protocol, Version 6 (IPv6) Specification          [DONE]
    §8.1 upper-layer checksums over the IPv6 pseudo-header. The
    synthesised ICMPv6 echo replies compute it correctly, and the UDP
    zero-checksum rule (a computed checksum of 0 MUST be transmitted as
    0xFFFF) is owned by the gVisor netstack since UDP moved to its
    forwarder.                                                        [DONE]
    §4 extension-header chain walking: gVisor walks the chain for TCP/UDP
    (forwarder-terminated); the NIC-intercepted ICMPv6 path now walks it
    too (Hop-by-Hop, Routing, Destination Options, Fragment — malformed
    chains dropped, other protocols passed through), and the fragment
    header's offset/M-flag/identification are decoded per §4.5 for
    reassembly and reply fragmentation.                               [DONE]

RFC 4443 — ICMPv6 for the IPv6 Specification                          [DONE]
    Echo Request/Reply (§4.1, §4.2) are handled correctly, checksum
    included.
    §3.2 Packet Too Big: generated by the yggdrasil ipv6rwc layer when an
    inbound frame exceeds the configured IfMTU (plumbed through since the
    gVisor migration), so PMTUD works on the Yggdrasil-facing leg —
    clients converge their path MTU after receiving it.
    §3.1 Destination Unreachable: ydn64 generates ICMPv6 errors for
    client-facing failures (undialable v4 UDP flows; kernel-received v4
    Port Unreachables surfaced asynchronously) and translates IPv4-side
    ICMP errors — including v4 PTB → v6 PTB — for tracked flows, so PMTUD
    toward IPv4 servers works (see RFC 7915 entry above for scope
    exclusions: TCP quotes, RFC 4884 extensions, and the fresh-TTL
    limitation on hop-by-hop traceroute).


B. NAT BEHAVIOURAL REQUIREMENTS — NORMATIVELY REFERENCED BY RFC 6146
--------------------------------------------------------------------
These BCPs are where RFC 6146's mapping, filtering and timer MUSTs
actually live. They were absent from the earlier version of this list.

RFC 4787 (BCP 127) — NAT Behavioral Requirements for Unicast UDP   [PARTIAL]
    REQ-1 Endpoint-Independent Mapping (UDP): one BIB entry with one
          shared outbound socket per client (addr, port); same external
          ip:port toward every destination                              [DONE]
    REQ-3 port parity / range preservation                            [TODO]
    REQ-5 mapping timer >= 2 min, default >= 5 min
         (default 300 s, Validate() floor 120 s); refreshed only by the
         client's outbound datagrams per REQ-5's own rule — inbound v4
         traffic never extends a mapping; the BIB entry lives on the
         same client-activity clock as its sessions                   [DONE]
    REQ-6 configurable timers                                         [DONE]
    REQ-8 filtering behaviour, configurable via Nat64UdpFiltering:
          "address-dependent" default (= RFC 6146 §5.2's mandated NAT64
          default), "address-and-port-dependent" optional. True
          endpoint-independent filtering (delivering datagrams from
          never-contacted senders) is not implemented — it would need
          raw injection onto the client leg                      [PARTIAL]
    REQ-9 hairpinning                                                  [N/A]

RFC 5382 (BCP 142) — NAT Behavioral Requirements for TCP                [DONE]
    Source of TCP_EST (2 h), TCP_TRANS (4 min) and TCP_INCOMING_SYN (6 s).
    REQ-5 idle timeout: configurable Nat64TcpTimeout, default 7440 s (the
          REQ-5 ESTABLISHED floor; also the Validate() floor since ydn64
          does not distinguish TCP states). Connections idle past it have
          both legs torn down, releasing their global/per-source slots;
          refreshed only by payload traffic in either direction — the
          keepalive probes each leg already runs never extend it.       [DONE]
    REQ-1/2/3 (EIM, full state machine, inbound SYN) are architectural. [N/A]

RFC 5508 (BCP 148) — NAT Behavioral Requirements for ICMP             [DONE]
    REQ-1 NAT-assigned ICMP Identifier (client IDs never exposed,
          replies demuxed by allocated ID)                             [DONE]
    REQ-2 ICMP Query session timeout >= 60 s (fixed 60 s)             [DONE]
    REQ-3/4 ICMP error translation: v4 errors quoting a tracked Echo
          slot or UDP session reach the client as translated ICMPv6
          errors (see RFC 7915 entry; TCP quotes excluded by design)   [DONE]
    REQ-10 ICMP hairpinning                                            [N/A]

RFC 8489 — Session Traversal Utilities for NAT (STUN)                  [N/A]
    Obsoletes RFC 5389. Not implemented by ydn64; listed as context for
    why RFC 4787 REQ-1 (Endpoint-Independent Mapping) matters — without
    it, ICE (RFC 8445) cannot form candidate pairs through ydn64.


C. DNS PROTOCOL RFCs — PULLED IN BY RFC 6147 §5.4
--------------------------------------------------
RFC 6147 §5.4 makes the synthesised response "subject to all the standard
DNS rules". These were absent from the earlier version of this list.

RFC 1034 / RFC 1035 — DNS concepts and implementation                 [DONE]
    Message format, TCP length-prefix framing, truncation and TC bit
    handling are correct. UDP size limit negotiated via EDNS(0).

RFC 3596 — DNS Extensions to Support IP Version 6                     [DONE]
    AAAA RR and §2.5 ip6.arpa nibble format. ydn64's PTR parser validates
    nibble count and character range.

RFC 6891 — Extension Mechanisms for DNS (EDNS(0))                  [PARTIAL]
    Obsoletes RFC 2671, which RFC 6147 §5.4 cites. UDP payload size
    negotiation and truncation implemented (§6.2.3-6.2.5): classic
    non-OPT queries are answered within 512 bytes (TC beyond), advertised
    sizes below 512 are treated as 512, and larger advertisements are
    clamped at 4096.     Outstanding:
      - EDNS(0) options (e.g., COOKIE, CLIENT-SUBNET)            [TODO]
      - DNSSEC support (DO bit, CD bit) — see RFC 6147 §5.5      [DONE]
        (minimum viable subset; ydn64 does not validate)

RFC 2308 — Negative Caching of DNS Queries (DNS NCACHE)               [DONE]
    Source of the SOA TTL used by RFC 6147 §5.1.7's TTL rule. The
    authority section of upstream responses is now passed through, so
    SOA-based negative caching works downstream.

RFC 5452 — Measures for Making DNS More Resilient against Forged
           Answers                                                 [PARTIAL]
    Source port is randomised by the OS. §9.2 query-ID unpredictability:
    upstream queries carry a ydn64-chosen random transaction ID — the
    client's own ID never leaves the node; answers whose ID does not
    match are discarded and the client's ID restored on return.       [DONE]
    0x20 name randomisation is optional hardening; not
    implemented.                                                      [TODO]

RFC 7766 — DNS Transport over TCP - Implementation Requirements    [PARTIAL]
    §3 TCP support and §6.2.1 idle timeouts are implemented.          [DONE]
    §6.2.1.1 concurrent processing of pipelined queries is not —
    queries on one connection are serialised.                         [TODO]

RFC 4074 — Common Misbehavior Against DNS Queries for IPv6 Addresses   [N/A]
    Informational; motivates RFC 6147 §5.1.2's RCODE handling rules.

RFC 4033 / 4034 / 4035 — DNSSEC                                  [PARTIAL]
    Required by RFC 6147 §5.5. Minimum viable subset implemented:
    queries with CD=1 && DO=1 are relayed upstream verbatim (no
    synthesis, no cache; a validating upstream's AD bit is preserved),
    and every response ydn64 generates or modifies clears the AD bit.
    ydn64 still performs no validation of its own — full validating
    DNS64 (vDNS64) is a much larger piece of work.                  [DEFERRED]

RFC 5358 (BCP 140) — Preventing Use of Recursive Nameservers in
                     Reflector Attacks                             [DONE]
    ydn64 is gated by AllowedSources and additionally rate-limits queries
    per source address (Dns64RateLimit, token bucket with a small burst,
    SIGHUP-reloadable). Over-budget UDP queries are refused with strict
    reply spacing — denials themselves cannot be amplified — and NAT64
    per-source ceilings keep one peer from monopolising translator state.

RFC 8482 — Providing Minimal-Sized Responses to DNS Queries that have
           QTYPE=ANY                                            [DEVIATION]
    ydn64 rewrites ANY to AAAA and applies normal synthesis rather than
    answering minimally, so IPv6-only clients get a usable answer.

RFC 7873 / RFC 9018 — DNS Cookies                                     [TODO]
    Optional anti-spoofing / anti-amplification hardening.

RFC 7871 — Client Subnet in DNS Queries                                [N/A]
    Optional. Not implemented; may degrade CDN geolocation through the
    configured forwarder.


D. PREFIX DISCOVERY AND CLIENT INTEGRATION
------------------------------------------

RFC 7050 — Discovery of the IPv6 Prefix Used for IPv6 Address
           Synthesis                                                  [DONE]
    Implemented intentionally: AAAA queries for ipv4only.arpa. are
    intercepted and resolved locally. Covered by unit tests.

RFC 8880 — Special Use Domain Name 'ipv4only.arpa'                    [DONE]
    Updates RFC 7050. §7.1 resolved: ipv4only.arpa. is handled locally
    for both A and AAAA queries instead of querying upstream.

RFC 8781 — Discovering PREF64 in Router Advertisements                 [N/A]
    Recent iOS/Android prefer this over RFC 7050. A TUN-less node has no
    L2 presence and cannot emit Router Advertisements, so such clients
    must fall back to RFC 7050 or be configured manually. This is a
    deployment-model limitation, not a code gap.

RFC 7225 — Discovering NAT64 IPv6 Prefixes Using PCP                   [N/A]
    Would require a PCP server on the Yggdrasil-facing side. Low value.

RFC 6877 — 464XLAT: Combination of Stateful and Stateless
           Translation                                                 [N/A]
    Context for CLAT-equipped clients reaching ydn64's PLAT function.

RFC 8305 — Happy Eyeballs Version 2                                    [N/A]
    Client-side. §7 covers DNS64/NAT64 interaction; relevant to
    understanding client behaviour, nothing to implement.


E. INFORMATIONAL / CONTEXT — NOTHING TO IMPLEMENT
--------------------------------------------------

RFC 6144 — Framework for IPv4/IPv6 Translation                         [N/A]
RFC 6889 — Analysis of Stateful 64 Translation                         [N/A]
RFC 7269 — NAT64 Deployment Options and Experience                     [N/A]
RFC 6586 — Experiences from an IPv6-Only Network                       [N/A]
RFC 6384 — An FTP ALG for IPv6-to-IPv4 Translation                     [N/A]
    Optional; would be needed only for active-mode FTP through NAT64.
RFC 6219 — CERNET2 Stateful IVI Translation Design                     [N/A]
RFC 7757 — Explicit Address Mappings for Stateless Translation         [N/A]
    Both apply to stateless/IVI variants; ydn64 is stateful.